# Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=techinsight

# Embedding Configuration
# ローカルモデル (sentence-transformers/all-MiniLM-L6-v2) を使用。外部APIキー不要。
EMBEDDING_PROVIDER=local
