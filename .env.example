# Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=techinsight

# Embedding Configuration
# local: ローカルモデル (sentence-transformers/all-MiniLM-L6-v2) を使用。APIキー不要。
# openai: OpenAI API を使用（未実装・将来対応予定）
EMBEDDING_PROVIDER=local

# OpenAI API (EMBEDDING_PROVIDER=openai の場合のみ必要)
# OPENAI_API_KEY=sk-...
